
public class warehousealgorithm {

	/*
	 * product locations for an order - put into an array
	 * when order is set as picked
	 * access the relevant array 
	 * (0,0 being origin)
	 * put in ascending order - by x, y then z
	 * provide locations in that order
	 * 
	 * String someString = "123"
	 * int i = Integer.parseInt( someString );

	 */	
 
//notes for travelling Ben Back algorithm - items given x and y co-ordinates based on integers parsed from their location strings, placed in ascending order and printed to the user in this order
}